# ── Data pipeline (pinned to CI-verified versions — bump alongside 90-day retune) ──
pandas==3.0.2
numpy==2.4.4
yfinance==1.3.0
fredapi==0.5.2
pytrends==4.9.2
requests==2.33.1
python-dotenv==1.2.2
beautifulsoup4==4.14.3
lxml==6.1.0

# ── ML ──
xgboost==3.2.0
scikit-learn==1.8.0
joblib==1.5.3

# ── App only (not used in CI daily pipeline) ──
streamlit>=1.30
plotly>=5.18

# ── Tuning only (not used in CI daily pipeline) ──
optuna>=3.5

# ── Testing ──
pytest>=7.0
